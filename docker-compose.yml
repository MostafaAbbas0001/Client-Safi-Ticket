services:
  client:
    build:
      context: .
      args:
        VITE_API_BASE_URL: "${VITE_API_BASE_URL:?Set VITE_API_BASE_URL in Client/.env}"
    image: safi-client
    container_name: safi_client
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"


